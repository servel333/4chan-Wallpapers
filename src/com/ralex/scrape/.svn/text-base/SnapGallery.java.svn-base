package com.ralex.scrape;

import android.content.Context;
import android.util.AttributeSet;
import android.widget.Gallery;

/* http://www.google.com/codesearch/p?hl=en#uX1GffpyOZk/core/java/android/widget/Gallery.java&q=android.widget.gallery&sa=N&cd=1&ct=rc&t=1 */
public class SnapGallery extends Gallery {

	public SnapGallery(Context context) {
		super(context);
		this.setUnselectedAlpha((float) 1);
		this.setAnimationDuration(1500);
		this.setPadding(100, 0, 10, 0);
	}

	public SnapGallery(Context context, AttributeSet attrs) {
		super(context, attrs);
	}

	public SnapGallery(Context context, AttributeSet attrs, int defStyle) {
		super(context, attrs, defStyle);
	}

//	public boolean onFling(MotionEvent e1, MotionEvent e2, float velocityX,
//			float velocityY) {
////		Log.i("MAGIC", "fling velocity: "+velocityX);
////		if(velocityX < 0){
////			Log.i("MAGIC", "simulating keypad up");
////			this.dispatchKeyEvent(new KeyEvent(KeyEvent.ACTION_UP, KeyEvent.KEYCODE_DPAD_UP));
////		}
////		else if(velocityX > 0){
////			Log.i("MAGIC", "simulating keypad down");
////			this.dispatchKeyEvent(new KeyEvent(KeyEvent.ACTION_UP, KeyEvent.KEYCODE_DPAD_DOWN));
////		}
//		return true;
//	}
}
